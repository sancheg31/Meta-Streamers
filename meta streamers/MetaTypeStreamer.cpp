#include "MetaTypeStreamer.h"

/*
MetaTypeStreamer::MetaTypeStreamer(const QMetaType& type): QObject(), metaType(type.id()) { }

void MetaTypeStreamer::setType(QMetaType type) {

}

const QMetaType& MetaTypeStreamer::getType() const {
    return metaType;
}

void MetaTypeStreamer::stream(QIODevice* device) const {

}

void MetaTypeStreamer::stream(const QMetaType& type, QIODevice* device) {
    MetaTypeStreamer streamer(type);
    streamer.stream(device);
}
*/
